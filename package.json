{
  "name": "christmas-market-neighborhood-map",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "mysql": "^2.18.1",
    "react": "^16.4.1",
    "react-dom": "^16.13.1",
    "react-google-maps": "^9.4.5",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "go-server": "npm run build && rm -rf server/static/* && mv build/static/* server/static/ &&  mv build/index.html server/static/"
  }
}
